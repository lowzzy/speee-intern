<nav class="navbar navbar-expand-lg navbar-light bg-light m-0 p-0">
  <div class="container-fluid m-0" style="background-color: #5CB04C; height: 60px;">
    <%= link_to home_path do %> <%= image_tag 'logo.png', class:'header-logo' %> <% end %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse row-right" id="navbarSupportedContent">
        <% if current_user.present? %>
        <%= link_to 'ログアウト', destroy_user_session_path, method: :delete, class:'text-white' %>
        <p class="text-success mb-0 p-3">|</p>
        <p class="text-white mb-0"><%= current_user.name %> さん</p>
        <%= image_tag 'animal_avatar.png', class:"rounded-circle avatar m-3"%>
        <% else %>
        <%= link_to '新規登録', new_user_registration_path, class:'nav-link active text-white fw-bold' %>
        <%= link_to 'ログイン', new_user_session_path, class:'nav-link active text-white' %>
        <% end %>
    </div>
  </div>
</nav>